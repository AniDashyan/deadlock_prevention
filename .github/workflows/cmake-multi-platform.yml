name: CMake Multi-Platform Build & Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up CMake
        uses: jwlawson/actions-setup-cmake@v1
        with:
          cmake-version: latest

      - name: Configure
        run: cmake -S . -B build

      - name: Build
        run: cmake --build build

      - name: Set executable path
        id: setpath
        run: |
          if [[ "${{ matrix.os }}" == "Windows" ]]; then
            echo "EXE=./build/deadlock.exe" >> $GITHUB_ENV
          else
            echo "EXE=./build/deadlock" >> $GITHUB_ENV
          fi
        shell: bash

      - name: Run Method 1 (safe single access)
        run: $EXE --method 1 --iters 1000
        shell: bash

      - name: Run Method 3 (scoped_lock)
        run: $EXE --method 3 --iters 1000
        shell: bash

      - name: Run Method 4 (ordered lock)
        run: $EXE --method 4 --iters 1000
        shell: bash

      - name: Run Method 2 (deadlock test)
        run: |
          echo "NOTE: Method 2 will demonstrate a deadlock and exit early."
          timeout 3 $EXE --method 2 || echo "Expected deadlock triggered"
        shell: bash
